services:
  - type: web
    name: rakib-esrgan-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python index.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.3
